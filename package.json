{
  "name": "dineposai-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-tenant restaurant POS and operating system",
  "scripts": {
    "dev": "pnpm --filter \"*\" dev",
    "build": "pnpm --filter \"*\" build",
    "start": "pnpm --filter \"*\" start",
    "test": "pnpm --filter \"*\" test"
  },
  "devDependencies": {
    "next": "16.2.7",
    "pg": "^8.22.0",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}

